# 实施状态

更新日期：2026-09-13

## 已完成

- M1：完成五个候选仓库的初筛、三个候选的源码级对比，并固定 Sleepy 上游提交。
- 保留完整上游 Git 历史，将上游远端命名为 `upstream`。
- Release 固定 `com.wedo.schedule`、Android API 26、`0.1.0` 和 `wedo课程表` 显示名；Debug 使用 `com.wedo.schedule.debug` 并显示为 `wedo课程表 Debug`，避免测试覆盖用户课表数据。
- 建立项目计划、架构、许可证、风险、JLJU 取证和发布文档。
- 建立首次启动隐私确认、本地数据不备份、HTTPS-only 网络安全配置。
- 收紧 WebView：不忽略证书错误、不允许混合内容或本地文件访问、外链交给系统浏览器、JS Bridge 仅允许证据确认的域名。
- 首版导航、权限和后台行为中关闭桌面组件、提醒、自动更新及调试注入入口。
- 移除 Release 使用 debug 签名的配置；正式密钥只允许由发布环境注入。
- 建立单元测试、Lint、Debug APK 和敏感信息扫描的 CI 基线。
- 用户已接受 Android SDK License；本地已安装 Platform 37.0、Build Tools 37.0.0 和 Platform Tools 37.0.1。
- 将 AGP 从 9.1.0 修正为官方支持 API 37.0 的最低版本 9.1.1。
- 本机构建门通过：1296 项 Debug 单元测试、`lintDebug` 和 `assembleDebug` 均成功。
- UI 方向确认为明亮通透的平衡档液态玻璃，默认蓝色强调色和悬浮 Dock，并建立 `docs/UI_DESIGN.md`。
- 完成 JLJU 已登录课表页的真实 HAR 取证，确认融合门户、教务域名、新版正方系统、学期选择器、课表接口以及日期/节次接口。
- 建立可重复的 HAR 脱敏脚本，显式区分正常课表与真实空课表，生成学期、请求结构和两类公开夹具；真实课程、教师、教室及所有认证值均未进入公开文件。
- 新增 `JlJuParser` 纯函数入口，复用基座 `JwNewZfParser` 解析真实 `kbList` 结构；JLJU 专项回归测试已通过，6 条响应记录正确生成 12 条分周课程记录。
- 完成真实空课表取证：同一接口返回 HTTP 200 JSON 且 `kbList` 为空；已加入离线空结果回归测试。
- 从门户服务目录确认 JLJU 官方教务 SSO 入口，并以无 Cookie 请求确认 `jwxt` 到 `lxr` 的前半段重定向；已生成不含票据值的公开结构夹具。
- 将 WebView 登录导航限制到 `jwxt`、`lxr`、`cas` 的最小职责集合；JS Bridge 仅在最终 `jwxt` 顶层页面动态安装，使用随机接口名并在离开教务域时移除。
- JLJU 学校目录已从“待适配”切换为可测试的新版正方入口，支持 WebView 登录后获取真实 `kbList`。
- 用户已在真机完成 JLJU 登录和课表导入，确认当前端到端主路径可用；该结论不替代尚缺的异常及会话过期取证。
- UI MVP 产品方向已完成访谈确认：完整七日周视图、浅色/深色蓝色液态玻璃、稳定彩色课程块、三段悬浮 Dock、通用添加入口、平衡密度和可关闭的弹簧动效；详细实施步骤见 `docs/UI_MVP_IMPLEMENTATION_PLAN.md`。
- UI MVP 首个可安装候选版已完成：启动后直达周视图、紧凑玻璃顶栏、左右切周/周选择、七日网格、稳定彩色课程块、冲突红色提示、下滑隐藏的紧凑悬浮 Dock、通用添加面板和浅深主题。
- 通用添加面板按“教务直连 → WakeUp 日历文件迁移 → 粘贴文本 → 通用文件导入 → 手动添加”组织，学校选择层不写死 JLJU，并继续复用原有 Parser 和导入预览。
- 设置页已按外观、课表显示、数据与隐私重组，可调整玻璃质量、动效/震动、密度、5/6/7 日和全局课程字段模板。
- 课程详情已增加小屏滚动；长按课程可换色、编辑、复制和删除单次记录。
- 冻结源码候选版通过 1304 项 Debug 单元测试、`lintDebug`、`assembleDebug`、`assembleDebugAndroidTest` 和敏感信息扫描；Lint 无 Error。
- MuMu Android 15 端 UI 冒烟测试覆盖切周、Dock 隐藏/恢复、课程详情、添加面板、设置页与深色模式；测试仅使用匿名课表并在结束后恢复原设置。

## 当前验收状态

- M1：通过。
- M2：通过。
- M3：进行中；正常与空课表数据面及 SSO 前半段已确认，交互登录和异常响应证据未齐。
- M4-M8：尚未通过；M4 的正常及空响应解析测试已经建立，但需等待 M3 验收门完整通过。

## 阻塞项

1. 尚缺 `lxr` JavaScript 中转之后的交互式登录回跳、验证码行为和会话过期响应。JLJU 入口仅作为端到端测试功能开放，不视为发布验收通过。
2. 正式 GitHub `origin`、发布签名 Secrets 和三名真实测试者尚未提供；这些只在后续发布阶段需要。

## 下一验收动作

- 产品方在真实课表上评审 UI MVP 的信息密度、色彩和 Dock 手感，再进入第二轮动效与细节调优。
- 补齐 API 26 实机/模拟器验证、字体缩放、5/6 日布局、WakeUp 导出的 ICS 日历文件真实样本和 JLJU 重新导入回归。
- JLJU 主路径已完成一次真机验证，发布验收仍需补采会话过期响应。
